// Progress reporting view is reserved for future expansion.
// The original ReportsView did not expose a progress report option,
// so this component intentionally renders nothing to preserve runtime behaviour.
export default function ProgressReportTab() {
  return null;
}
